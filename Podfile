# Uncomment the next line to define a global platform for your project
platform :ios, '13.0'

target 'Codegen' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Codegen
  pod 'Alamofire', '~> 4.9.0'
  pod 'SwaggerClient', :path => './codegen-api'

  target 'CodegenTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CodegenUITests' do
    # Pods for testing
  end

end
